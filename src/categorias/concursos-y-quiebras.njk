---
layout: layouts/page.njk
title: Concursos y Quiebras
description: Contenido sobre insolvencia, verificación de créditos, privilegios, sindicatura, quiebra y procesos concursales.
---
<div class="article-list">
  {% for post in collections.articulos %}
    {% if post.data.category == "Concursos y Quiebras" %}
      <article>
        <p class="article-meta">{{ post.date | fechaES }}</p>
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        <p>{{ post.data.description }}</p>
        <a class="read-more" href="{{ post.url }}">Leer análisis →</a>
      </article>
    {% endif %}
  {% endfor %}
</div>
